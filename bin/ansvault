#!/bin/bash
#
# used by ansible-vault via $ANSIBLE_VAULT_PASSWORD_FILE
# see gpg-agent.conf for caching params
# see bin/gpg wrapper for how it works when arriving via sudo use_pty
#
~/bin/gpg -q --decrypt ${ANSVAULT_PGPFILE:?} 2>/dev/null
