#

[storage]
driver = "overlay"
runroot = "/run/containers/storage"

# this path is specific to kairos
graphroot = "/usr/local/var/lib/containers/storage"

[storage.options.overlay]
mount_program = "/usr/bin/fuse-overlayfs"
